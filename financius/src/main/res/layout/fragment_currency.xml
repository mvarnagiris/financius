<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <RelativeLayout
        android:id="@+id/container_V"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:animateLayoutChanges="true"
        android:background="@color/brand_primary"
        android:elevation="@dimen/elevation_header"
        android:orientation="horizontal"
        android:paddingBottom="@dimen/space_xlarge"
        android:paddingLeft="@dimen/space_large"
        android:paddingRight="@dimen/space_large"
        android:paddingTop="@dimen/space_xlarge"
        tools:ignore="UnusedAttribute">

        <TextView
            android:id="@+id/code_TV"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/Text.Headline.Inverse"
            android:viewName="code"
            tools:text="GBP → USD" />

        <TextView
            android:id="@+id/exchangeRate_TV"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/space_normal"
            android:layout_toRightOf="@id/code_TV"
            android:gravity="right"
            android:textAppearance="@style/Text.Headline.Inverse.Secondary"
            tools:text="1.2489" />

        <TextView
            android:id="@+id/format_TV"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/code_TV"
            android:layout_marginTop="@dimen/space_normal"
            android:textAppearance="@style/Text.Headline.Inverse.Secondary"
            tools:text="1,000.00 £" />

    </RelativeLayout>

    <fr.castorflex.android.smoothprogressbar.SmoothProgressBar
        android:id="@+id/loading_SPB"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:visibility="invisible" />

    <View
        android:id="@+id/separator_V"
        style="@style/Divider.Thin"
        android:layout_below="@id/container_V" />

    <ScrollView
        style="@style/ScrollView"
        android:layout_below="@id/separator_V">

        <LinearLayout
            android:id="@+id/accountsContainer_V"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:animateLayoutChanges="true"
            android:divider="?android:attr/dividerVertical"
            android:orientation="vertical"
            android:showDividers="middle|end" />

    </ScrollView>

</RelativeLayout>